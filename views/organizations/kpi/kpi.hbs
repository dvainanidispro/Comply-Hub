<script>
    window.kpis = {{{objectify kpis}}};
    console.log('kpis:');
    console.log(window.kpis);
</script>



<div class="row g-4 mt-2">

    {{!-- ===== ΣΧΕΔΙΟ 1: Compact Table ===== --}}
    <div class="col-12">
        <div class="card">
            <div class="card-header d-flex align-items-center gap-2">
                <i class="bi bi-table"></i>
                <h5 class="mb-0 fw-semibold">Σχέδιο 1 — Πίνακας - {{kpis.length}}</h5>
            </div>
            <div class="card-body">
                {{> kpi/table kpis=kpis links=true }}
            </div>
        </div>
    </div>

    {{!-- ===== ΣΧΕΔΙΟ 2: Metric Cards Grid ===== --}}
    <div class="col-12">
        <div class="card">
            <div class="card-header d-flex align-items-center gap-2">
                <i class="bi bi-grid-3x3-gap"></i>
                <h5 class="mb-0 fw-semibold">Σχέδιο 2 — Metric Cards - {{kpis.length}}</h5>
            </div>
            <div class="card-body">
                {{> kpi/grid kpis=kpis links=true }}
            </div>
        </div>
    </div>

</div>

{{> kpi/dialog kpis=kpis }}