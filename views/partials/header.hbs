    <div class="header navbar" >
        <div class="header-container p-5">

            <ul class="nav-left">
                <li>
                    <a id="sidebar-toggle" class="sidebar-toggle" href="javascript:void(0);">
                        <i class="ti-menu"></i>
                    </a>
                </li>
            </ul>

            <ul class="nav-center">
                {{#if (can "manage:any:content")}}
                <a href="/account/settings{{#if currentPath}}?redirect={{currentPath}}{{/if}}" class="h3 text-secondary m-0">{{org.name}}</a>
                {{else}}
                <div class="h3 text-secondary m-0">{{org.name}}</div>
                {{/if}}
            </ul>

            <ul class="nav-right">

                <li class="dropdown if-user">
                    <a href="" class="dropdown-toggle no-after peers fxw-nw ai-c lh-1" data-bs-toggle="dropdown">
                        <div class="d-flex align-items-center justify-content-between p-2">
                            <div class="peer">
                                <span class="fsz-sm" data-variable="name">{{user.name}}</span>
                            </div>
                            <div class="avatar">
                            <wa-avatar label="User avatar" name="{{user.name}}"></wa-avatar>

                            </div>
                        </div>
                    </a>

                    <nav class="user-nav" aria-label="User menu">
                        <ul class="dropdown-menu fsz-sm me-3">
                            <li>
                                <a href="/account/profile" class="d-b td-n pY-5 bgcH-grey-100 c-grey-700">
                                    <i class="ti-user mR-10"></i>
                                    <span>Προφίλ</span>
                                </a>
                            </li>
                            {{#if (can "manage:any:content")}}
                            <li>
                                <a href="/account/settings" class="d-b td-n pY-5 bgcH-grey-100 c-grey-700">
                                    <i class="ti-settings mR-10"></i>
                                    <span>Ρυθμίσεις</span>
                                </a>
                            </li>
                            {{/if}}
                            <li role="separator" class="divider"></li>
                            <li><a href="/logout" class="d-b td-n pY-5 bgcH-grey-100 c-grey-700 btn-logout">
                                    <i class="ti-power-off mR-10"></i>
                                    <span>Αποσύνδεση</span>
                                </a>
                            </li>
                        </ul>
                    </nav>
                    
                </li>
            </ul>
        </div>
    </div>